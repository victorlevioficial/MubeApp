{
  "name": "mube-firebase-rules-tests",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24"
  },
  "scripts": {
    "test": "firebase emulators:exec --config ../firebase.json --project mube-rules-test --only firestore,storage \"npm run test:all\"",
    "test:all": "npm run test:rules && npm run test:cleanup",
    "test:rules": "node --test test/firestore.rules.test.mjs test/storage.rules.test.mjs",
    "test:cleanup": "npm --prefix ../functions run build && npm --prefix ../functions run test:emulator"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.17.1",
    "firebase-tools": "15.26.0"
  }
}
